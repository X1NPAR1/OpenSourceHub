<Page x:Class="OpenSourceHub.UI.Views.Pages.FavoritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:loc="clr-namespace:OpenSourceHub.Localization;assembly=OpenSourceHub.Localization"
      Background="{StaticResource BackgroundBrush}" Title="Favorites">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,20">
            <TextBlock Text="{loc:Localization Favorites.Title}" Style="{StaticResource HeadingStyle}"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,24">
                <StackPanel Visibility="{Binding Items.Count, Converter={StaticResource ZeroToVisibilityConverter}}"
                            HorizontalAlignment="Center" Margin="0,80">
                    <TextBlock Text="⭐" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock Text="{loc:Localization Favorites.Empty}" Style="{StaticResource CaptionStyle}"
                               HorizontalAlignment="Center" FontSize="14" TextAlignment="Center"/>
                </StackPanel>

                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding FullName}" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{StaticResource BrandPrimaryBrush}" Cursor="Hand"/>
                                        <TextBlock Text="{Binding Description}" Style="{StaticResource CaptionStyle}"
                                                   Margin="0,4,0,0" TextTrimming="CharacterEllipsis"
                                                   Visibility="{Binding Description, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <TextBlock Text="{Binding Language}" FontSize="11"
                                                       Foreground="{StaticResource SubTextBrush}" Margin="0,0,16,0"
                                                       Visibility="{Binding Language, Converter={StaticResource NullToVisibilityConverter}}"/>
                                            <TextBlock Text="⭐" FontSize="11" Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding Stars, Converter={StaticResource NumberToKiloConverter}}"
                                                       FontSize="11" Foreground="{StaticResource SubTextBrush}" Margin="0,0,16,0"/>
                                            <TextBlock Text="{Binding AddedAt, StringFormat='Added {0:MMM dd, yyyy}'}"
                                                       FontSize="11" Foreground="{StaticResource SubTextBrush}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Note}" FontSize="12" Foreground="{StaticResource BrandAccentBrush}"
                                                   Margin="0,6,0,0" FontStyle="Italic"
                                                   Visibility="{Binding Note, Converter={StaticResource NullToVisibilityConverter}}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top">
                                        <Button Content="↗ Open" Style="{StaticResource GhostButtonStyle}"
                                                Command="{Binding DataContext.OpenRepositoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}" Margin="0,0,0,4" FontSize="12"/>
                                        <Button Content="🗑 Remove" Style="{StaticResource GhostButtonStyle}"
                                                Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}" FontSize="12"
                                                Foreground="{StaticResource ErrorBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
